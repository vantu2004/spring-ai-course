spring:
  application:
    name: mcpserverstdio
  datasource:
    url: jdbc:h2:file:C:\Users\tulev\OneDrive\Desktop\chatmemory;AUTO_SERVER=true
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
  ai:
    mcp:
      server:
        stdio: true

  main:
    # tắt web application
    web-application-type: none
    # tắt cái banner springboot khi chạy app lên
    banner-mode: off

logging:
  level:
    root: ERROR
